# AI agents plan

Future AI capabilities are isolated behind `AiAssistantService`. Initial use cases are error analysis and response explanation. The application must obtain explicit user consent before forwarding payload content; no provider integration or data forwarding is included in Phase 1.
