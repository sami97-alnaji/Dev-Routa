# Phase 4 test plan

All tests use deterministic local fixtures. Current coverage includes AST operation discovery and validation, GraphQL HTTP POST/GET, typed partial errors, local subscription handshake/ack/next, schema parsing/hash, and schema diff. Remaining mandatory coverage includes auth/environment/secure references, cancellation isolation, introspection denial, persistence migrations and retention, desktop/mobile flows, large Unicode responses, exports, reconnect behavior, and Windows/Android builds.

Status: IN PROGRESS.
