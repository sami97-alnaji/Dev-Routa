# Automation foundation

This slice validates definitions only. It has no scheduler, background service, webhook, or server. Manual triggers are valid; all automation has explicit limits and production is denied by default.
