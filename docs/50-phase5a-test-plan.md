# Phase 5A test plan

Tests use only fake adapters and process runners. They cover typed dispatch, default-deny permissions, approvals, registry metadata, safe orchestration, audit ordering, automation validation, and process-policy rejection. No external process, network, browser, or credential store is accessed.
